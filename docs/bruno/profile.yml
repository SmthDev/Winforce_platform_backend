info:
  name: profile
  type: http
  seq: 7

http:
  method: GET
  url: '{{stage_url}}/profile'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
