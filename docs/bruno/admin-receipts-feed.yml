info:
  name: admin-receipts-feed
  type: http
  seq: 22

http:
  method: GET
  url: '{{stage_url}}/balance/receipts/all?status=credited&limit=50&offset=0'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
