info:
  name: me
  type: http
  seq: 4

http:
  method: GET
  url: '{{stage_url}}/auth/me'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
