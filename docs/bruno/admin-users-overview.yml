info:
  name: admin-users-overview
  type: http
  seq: 33

http:
  method: GET
  url: '{{stage_url}}/balance/users?q=&limit=50&offset=0'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
