info:
  name: games-list
  type: http
  seq: 30

http:
  method: GET
  url: '{{stage_url}}/games?limit=50&offset=0'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
