info:
  name: telegram-get
  type: http
  seq: 27

http:
  method: GET
  url: '{{stage_url}}/profile/telegram'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
