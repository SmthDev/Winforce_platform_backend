info:
  name: admin-user-balance
  type: http
  seq: 23

http:
  method: GET
  url: '{{stage_url}}/balance/users/1'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
