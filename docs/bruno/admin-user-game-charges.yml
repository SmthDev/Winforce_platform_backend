info:
  name: admin-user-game-charges
  type: http
  seq: 31

http:
  method: GET
  url: '{{stage_url}}/balance/users/1/game-charges?limit=50&offset=0'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
