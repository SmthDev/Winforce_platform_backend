info:
  name: admin-user-transactions
  type: http
  seq: 24

http:
  method: GET
  url: '{{stage_url}}/balance/users/1/transactions?limit=50&offset=0'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
