info:
  name: signin
  type: http
  seq: 3

http:
  method: POST
  url: "{{stage_url}}/login"
  headers:
    - name: Content-Type
      value: application/json
    - name: Origin
      value: "{{origin}}"
  body:
    type: json
    data: |-
      {
        "email": "alex.zahreshchanka@gmail.com",
        "password": "Alex.zahreshchanka21!",
        "remember_me": true
      }
  auth: inherit

runtime:
  variables:
    - name: stage_url
      value: localhost:8081/api/v1
    - name: origin
      value: http://192.168.1.3:5173

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
