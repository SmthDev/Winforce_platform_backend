info:
  name: signin
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/login"
  headers:
    - name: Content-Type
      value: application/json
    - name: Origin
      value: "{{origin}}"
  body:
    type: json
    data: |-
      {
        "email": "alex.zahreshchanka@gmail.com",
        "password": "Alex.zahreshchanka21!",
        "remember_me": true
      }
  auth: inherit

runtime:
  variables:
    - name: stage_url
      value: api.platform.winforce.tech/api/v1
    - name: origin
      value: https://api.platform.winforce.tech/
    - name: host
      value: https://api.platform.winforce.tech/api/v1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
