info:
  name: signup
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/signup/credential
  headers:
    - name: Content-Type
      value: application/json
    - name: Origin
      value: "{{origin}}"
  body:
    type: json
    data: |-
      {
        "email": "alex.zahreshchanka@gmail.com",
        "password": "Alex.zahreshchanka21!"
      }
  auth: inherit

runtime:
  variables:
    - name: stage_url
      value: localhost:8081/api/v1
    - name: origin
      value: http://192.168.1.3:5173

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
