info:
  name: signup
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/auth/signup/credential"
  headers:
    - name: Content-Type
      value: application/json
    - name: Origin
      value: "{{origin}}"
  body:
    type: json
    data: |-
      {
        "email": "ksksn@gmail.com",
        "password": "Aleksandra81!"
      }


      // {
      //   "email":"leshenkov2007@gmail.com",
      //   "password": "Sasha20062007!"

      // }
  auth: inherit

runtime:
  variables:
    - name: stage_url
      value: localhost:8081/api/v1
    - name: origin
      value: https://api.platform.winforce.tech

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
