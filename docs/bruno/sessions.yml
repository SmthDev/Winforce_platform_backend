info:
  name: sessions
  type: http
  seq: 8

http:
  method: GET
  url: '{{stage_url}}/auth/sessions'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
