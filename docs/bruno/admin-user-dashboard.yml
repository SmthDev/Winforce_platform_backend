info:
  name: admin-user-dashboard
  type: http
  seq: 34

http:
  method: GET
  url: '{{stage_url}}/balance/users/1/dashboard?limit=20'
  auth: inherit
  headers:
    - name: Origin
      value: '{{origin}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
